name: fluttertry
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dart_openai: ^4.0.0
  google_fonts: ^6.1.0
  image_picker: ^1.0.4
  image: ^4.1.7

flutter:
  uses-material-design: true
  assets:
    - lib/config/.env

platforms:
  web: true
